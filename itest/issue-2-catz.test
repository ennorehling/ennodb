#!/bin/sh
exit 0 # test disabled
curl -s --data '<img src="http://placekitten.com/g/240/300/" />' http://localhost/ennodb/cat
curl -s --data '<img src="http://placekitten.com/g/200/300/" />' http://localhost/ennodb/catz
output=`curl -s http://localhost/ennodb/cat`
if [ -z $output ]; then
  echo "ERROR: expected a value for 'cat'"
  exit 1
fi
